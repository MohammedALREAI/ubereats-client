import React from 'react'
import { render } from '@testing-library/react'
import { Header } from '../header'


describe('should be render header ', () => {
    test('should  redner with that have props', () => {
        // const {}=render(<Header/>)
        
    })
    
})
